#lang racket
; дефиниция на списък
; сходство с индукция
; сглобяване vs разглобяване
; шаблон с let дефиниции
;
; хетерогенни
;
; аналог на типовете
; list?
; null?
;
; всичко в Scheme е списък
; по-подразбиране всичко се оценява (и се прилагат функции), с ' и quote спираме оценяването
;
; eq?, eqv?, equal? - when in doubt use equal?
; memq, memv, member
;






; map & filter - bread and butter
