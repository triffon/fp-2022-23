-- signum е функция, която приема 1 аргумент,
-- който е число (което може да бъде сравнявано, има наредба)
-- и връща резултат - също число
-- signum' :: (Ord a1, Num a1, Num a2) => a1 -> a2
signum' x
  | x > 0 = 1
  | x == 0 = 0
  | otherwise = -1